<div class="container-fluid">
  <div class="row justify-content-between align-items-center" id="header">
    <!-- <div class="col-4">
      <p class="h4 pt-2">{{ title }}</p>
    </div> -->
    <div class="col">
      <nav class="navbar navbar-expand-sm" style="padding: 0 !important">
        <a href="" class="navbar-brand">{{ title }}</a>
        <button
          class="navbar-toggler"
          data-toggle="collapse"
          data-target="#collapsibleNavbar"
          aria-expanded="true"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            viewBox="0 0 30 30"
            width="30"
            height="30"
            focusable="false"
          >
            <title>Menu</title>
            <path
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-miterlimit="10"
              d="M4 7h22M4 15h22M4 23h22"
              style="stroke: #5fb0e4"
            ></path>
          </svg>
        </button>
        <div
          class="navbar-collapse collapse justify-content-end"
          id="collapsibleNavbar"
          aria-expanded="true"
        >
          <ul class="navbar-nav">
            <li class="nav-item">
              <a href="/" target="_blank" class="nav-link">Timer Converter</a>
            </li>
            <li class="nav-item">
              <a href="/base64encodedecode" target="_blank" class="nav-link"
                >Base64 Encode/Decode</a
              >
            </li>
            <li class="nav-item">
              <a href="/urlencodedecode" target="_blank" class="nav-link"
                >URL Encode/Decode</a
              >
            </li>
            <li class="nav-item">
              <a href="/binaryconverter" target="_blank" class="nav-link"
                >Binary Converter</a
              >
            </li>
          </ul>
        </div>
      </nav>
    </div>
  </div>
  {{#if timestamp}}
  {{> timestamp}}
  {{/if}}
  {{#if base64}}
  {{> base64}}
  {{/if}}
  {{#if url}}
  {{> url}}
  {{/if}}
  {{#if binary}}
  {{> binary}}
  {{/if}}
</div>
